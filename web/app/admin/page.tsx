export const metadata = { title: 'Equipe — Admin Diamantes' };

export default function AdminHome() {
  return (
    <section className="page">
      <span className="eyebrow">Painel interno</span>
      <h1>Em migração 🚧</h1>
      <p className="muted">
        Equipe (admins + operadores), alunos, assinaturas/financeiro, projetos e demandas serão
        portados nas próximas fases.
      </p>
    </section>
  );
}
