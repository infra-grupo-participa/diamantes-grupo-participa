export const metadata = { title: 'Operador — Diamantes' };

export default function OperatorHome() {
  return (
    <section className="page">
      <span className="eyebrow">Área do operador</span>
      <h1>Em migração 🚧</h1>
      <p className="muted">
        Dashboard (workload, urgentes, avaliações, pontos), demandas atribuídas e perfil do operador
        serão portados nas próximas fases.
      </p>
    </section>
  );
}
