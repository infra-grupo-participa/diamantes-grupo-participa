export const metadata = { title: 'Portal — Diamantes' };

export default function PortalHome() {
  return (
    <section className="page">
      <span className="eyebrow">Portal do cliente</span>
      <h1>Em migração 🚧</h1>
      <p className="muted">
        Dashboard, briefing (básico e por projeto), demandas, projetos e perfil serão portados nas
        próximas fases.
      </p>
    </section>
  );
}
