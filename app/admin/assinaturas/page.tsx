import AssinaturasClient from '@/components/admin/AssinaturasClient';

export const metadata = { title: 'Assinaturas — Admin' };

export default function AssinaturasPage() {
  return <AssinaturasClient />;
}
